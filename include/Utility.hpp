#ifndef ARDUINO_FEATHER_UTILITY_HPP
#define ARDUINO_FEATHER_UTILITY_HPP

namespace Maze {
	const char* is(bool ok);
	double clamp(double v, double lo, double hi);
}

#endif //ARDUINO_FEATHER_UTILITY_HPP
